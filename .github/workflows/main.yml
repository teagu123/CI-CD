name: CI Study

on:
    push:
      branches: [ "main" ]

  jobs:
    build:
      runs-on: ubuntu-latest

      steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: my First Step
        run:
            npm install
            npm build
